/** Shell scrollabile con scrollbar webkit blue-glass (come catalogo materiali). */
export const PAGE_SCROLL_SHELL =
  "h-dvh overflow-y-auto overscroll-contain scrollbar-app"

/**
 * Wrapper orizzontale per tabelle desktop.
 * touch-pan-x: lo scroll verticale resta sulla pagina (niente overscroll-contain che lo mangia).
 */
export const TABLE_SCROLL_X =
  "overflow-x-auto touch-pan-x scrollbar-app"
